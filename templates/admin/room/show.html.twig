{% extends 'base.html.twig' %}

{% block title %}Room{% endblock %}

{% block body %}
    <h1>Room</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ room.name }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ room.adress }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ room.ville }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ room.region }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ room.latitude }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ room.capacity }}</td>
            </tr>
            <tr>
                <th>Longtitude</th>
                <td>{{ room.longtitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_room_index') }}">back to list</a>

    <a href="{{ path('app_room_edit', {'id': room.id}) }}">edit</a>

    {{ include('room/_delete_form.html.twig') }}
{% endblock %}
